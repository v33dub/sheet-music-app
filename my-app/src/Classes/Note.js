export default class Note {
    constructor(letter, octave, isSharp, isFlat) {
        this.letter = letter;
        this.octave = octave;
        this.isSharp = isSharp;
        this.isFlat = isFlat;
    }
}